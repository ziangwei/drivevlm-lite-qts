# Archived tests

Tests for code in `scripts/archive/` or `src/drivevlm_lite/` modules that have
been deprecated. Excluded from regular `pytest tests/` runs (see project
test config).

If a module is brought back out of archive, move the corresponding test
file back to `tests/` as well.
