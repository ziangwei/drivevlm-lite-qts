# Minimal packages for E0 zero-shot baseline and data preparation.
# Install torch/torchvision/torchaudio separately from the PyTorch CUDA wheel.
datasets>=3.0.0
huggingface_hub[cli]>=0.34.0
pillow>=10.0.0
pyyaml>=6.0.0
qwen-vl-utils[decord]>=0.0.10
rich>=13.0.0
tqdm>=4.66.0
transformers>=4.57.0
